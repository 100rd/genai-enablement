# Vendored Skill — Provenance

- **Skill:** `terraform-test` (registry path `engineering/terraform-test`)
- **Upstream:** `hashicorp/agent-skills` · path `terraform/code-generation/skills/terraform-test`
- **Author:** HashiCorp (copyright notice: Copyright IBM Corp. 2026)
- **License:** MPL-2.0 (see `LICENSE`)
- **Vendored from:** monorepo `.claude/skills/terraform-test` (originally vendored from upstream 2026-06-10)
- **Vendored into platform registry:** 2026-07-07

Carried verbatim. Upstream `SKILL.md` frontmatter has a `metadata` block (copyright + version
0.0.2) which is preserved; only the platform extension fields (`version`, `tags`,
`compatible_tools`, `tier`, `license`) were added. Body and references are unchanged.

**MPL-2.0 terms:** weak, file-level copyleft. The `LICENSE` file and all file headers/notices
must stay intact — do not strip them. If any MPL-covered file is modified in place, that file's
source must remain available under MPL-2.0. This skill is vendored **unmodified** except the
frontmatter platform-field additions above, so the obligation is satisfied by keeping `LICENSE`
and notices in place.

**Refresh:** re-pull from upstream and re-verify the `LICENSE` body before trusting. Provenance
authority for the wider monorepo set is `.claude/skills/VENDORED.md`.
